<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Config for AxiomNode.
    Allows cleartext (HTTP) traffic ONLY to the Android emulator loopback
    (10.0.2.2) and localhost for local development.
    All other domains require HTTPS by default.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback to host machine -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Localhost (physical device via adb reverse) -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>

